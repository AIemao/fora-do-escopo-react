const Footer = () => {
  return (
    <footer style={styles.footer}>
      
    </footer>
  );
};

const styles = {
  footer: {
    color: '#fff',
    padding: '10px',
    textAlign: 'right',
  },
  description: {
    margin: 0,
  },
};

export default Footer;
